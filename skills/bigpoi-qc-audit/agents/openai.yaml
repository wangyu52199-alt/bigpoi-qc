interface:
  display_name: "Big POI QC Auditor"
  short_description: "Review Big POI verification with strict input and output schemas"
  default_prompt: "Normalize the upstream Big POI verification payload to the input schema, review whether the evidence supports the upstream result, and return a JSON object that strictly matches the output schema."

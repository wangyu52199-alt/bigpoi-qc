# shared

本目录提供闭环系统的公共能力，供 5 个 skill 复用：

1. 统一 schema（人工结果、分流结果、自我迭代输出、回归输入输出）
2. taxonomy（标签、分流类别、模块类别、优先级）
3. utils（校验、规则加载、路由辅助、分桶、Markdown 报告）
4. examples（覆盖关键业务场景的样本）
5. tests（最小可运行校验）

设计原则：

- 字段名严格沿用输入定义，不擅自重命名
- 人工输入仅为“现象+判断+证据”，不包含整改建议
- 规则配置外置，可回放、可追踪
